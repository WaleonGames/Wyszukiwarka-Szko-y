<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Szczegóły Zgłoszenia - Panel Administratora</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <%- include('partials/aside') %> <!-- Menu boczne (aside) -->

            <!-- Główna część (content) -->
            <main class="col-md-9">
                <div class="container mt-5">
                    <h2>Szczegóły Zgłoszenia</h2>
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><%= szkola.nazwa %> - <%= szkola.miasto %></h5>
                            <h6 class="card-subtitle mb-2 text-muted">Opis Zgłoszenia</h6>
                            <p class="card-text"><%= zgloszenie.opis %></p>
                            <p><strong>Data zgłoszenia:</strong> <%= new Date(zgloszenie.data_zgloszenia).toLocaleString('pl-PL', { weekday: 'long', hour: '2-digit', minute: '2-digit' }) %></p>
                            <p><strong>Szkoła:</strong> <%= szkola.nazwa %> (<%= szkola.miasto %>)</p>
                            <a href="/admin/zgloszenie/<%= zgloszenie.id %>/delete" class="btn btn-danger btn-sm">
                                <i class="fas fa-trash"></i> Usuń
                            </a>
                            <a href="/admin/schools" class="btn btn-secondary btn-sm">
                                <i class="fas fa-arrow-left"></i> Powrót do listy szkół
                            </a>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
