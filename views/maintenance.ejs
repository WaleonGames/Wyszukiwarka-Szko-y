<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Witryna w Przerwie Technicznej</title>
    <!-- Dodajemy CDN Bootstrapa -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Dodatkowy styl dla zaokrąglonych rogów -->
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .header-message {
            text-align: center;
            margin-bottom: 30px;
        }
        .header-message h1 {
            font-size: 3rem;
            color: #dc3545;
        }
        .social-links a {
            margin: 10px;
            font-size: 1.2rem;
        }
        .card {
            border-radius: 12px;
        }
        .card-body p {
            font-size: 1.1rem;
        }
        .btn-outline {
            border-radius: 20px;
        }
        /* Stylizacja dla komunikatu cookie */
        .cookie {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background-color: #f8f9fa;
            padding: 15px;
            text-align: center;
            border-top: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Główna wiadomość -->
        <div class="header-message">
            <h1>Witryna jest obecnie niedostępna</h1>
            <p class="lead"><%= message %></p>
        </div>

        <!-- Więcej informacji o statusie -->
        <div class="card shadow-lg w-75">
            <div class="card-header text-white text-center" style="background-color: #343a40;">
                <h4>Więcej informacji</h4>
            </div>
            <div class="card-body">
                <p class="text-center">Dowiedz się więcej o statusie witryny:</p>
                <div class="social-links text-center">
                    <a href="https://facebook.com" target="_blank" class="btn btn-outline-primary btn-lg">
                        <i class="fab fa-facebook-square"></i> Facebook
                    </a>
                    <a href="https://discord.com" target="_blank" class="btn btn-outline-secondary btn-lg">
                        <i class="fab fa-discord"></i> Discord
                    </a>
                    <a href="https://twitter.com" target="_blank" class="btn btn-outline-info btn-lg">
                        <i class="fab fa-twitter"></i> Twitter
                    </a>
                </div>
                <p class="mt-3 text-center">Przepraszamy za niedogodności!</p>
            </div>
        </div>
    </div>
    
    <!-- Komunikat o cookies -->
    <%- include('partials/cookie') %>

    <!-- Skrypt Bootstrapa -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- FontAwesome CDN dla ikon -->
    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
</body>
</html>
